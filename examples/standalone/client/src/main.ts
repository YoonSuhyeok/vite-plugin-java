import './style.css'

document.querySelector<HTMLDivElement>('#app')!.innerHTML = `
  <div>
    <h1>Vite + Java (Standalone)</h1>
    <p>This example uses a standalone vite.config.ts without vite-plugin-java.</p>
  </div>
`
